# gcovr configuration file
# Restrict to repo source (src/ + inc/) and exclude tests/subprojects

gcov-ignore-errors = no_working_dir_found

# Include only project source directories (repo-only; matches branch-coverage-limitation.md).
filter = src/
filter = inc/

# Exclude test directories
exclude = .*/test/.*

# Exclude subprojects directory
exclude = .*/subprojects/.*

# Exclude test and mock files anywhere
exclude = .*/test_.*
exclude = .*/mock_.*

# Exclude system headers
exclude = /usr/.*

exclude = src/main\.cpp

# Exclude compiler-generated exception-handling branches (e.g. copy/move
# constructors of std containers) that are not reachable in normal code paths.
exclude-throw-branches = yes

# Exclude branches in lines that are not reachable (compiler dead code).
exclude-unreachable-branches = yes

decisions = yes

output = coverage.html
html = yes
print-summary = yes
